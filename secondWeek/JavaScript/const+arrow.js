const name = 'Rayhan';
const age = 45;
const hobby = true;

const towhid = (bothname, bothage, huda) => {
    return (
        'Name is: ' + bothname + ', Their both age: ' + bothage + ', His Hobby Has a Meaning: ' + huda
    );
}

console.log(towhid(name, age, hobby));

const add = (r, i) => ('The sum is: '+ (r + i));

console.log(add(7,25));
