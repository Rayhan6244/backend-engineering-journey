const name = 'Rayhan';
const age = 23;
const commit = true;

function Towhid(username, userage, usercommit){
    return (
        'Name is: ' + username + 'His age is: ' + userage + ', He is loyal: ' + usercommit
    );
}

console.log(Towhid(name, age, commit));
