const name = 'Rayhan and Isha';
const age = 45;
const marriage = true;

const towhid = (bothname, bothage, bothmarried) => {
    return (
        'Their Name is: ' + bothname + ', Their both age: ' + bothage + ', Are they married?: ' + bothmarried
    );
}

console.log(towhid(name, age, marriage));

const add = (r, i) => ('The sum is: '+ (r + i));

console.log(add(7,25));
